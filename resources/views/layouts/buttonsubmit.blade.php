                {{-- Buttons --}}
<div class="col-span-1 md:col-span-2">
        <div class="flex flex-col sm:flex-row justify-between gap-4 sm:gap-3 mt-6">
    <!-- Left side -->
            <button 
                id="backBtn"
                class="w-full sm:w-auto px-8 py-3 border-2 border-gray-400 text-gray-700 font-semibold rounded 
                        hover:bg-gray-50 transition-colors duration-200">
                        Back
            </button>
        <!-- Right Side-->
        <div class="flex flex-col sm:flex-row gap-4 sm:gap-3 justify-end">
            <button 
                id="saveBtn"
                    class="w-full sm:w-auto px-8 py-3 border-2 border-gray-400 text-gray-700 font-semibold rounded 
                    hover:bg-gray-50 transition-colors duration-200">
                    Save
            </button>
            <button
                id="nextBtn"
                disabled
                    class="w-full sm:w-auto px-8 py-3 bg-[#0E6021] text-white font-semibold rounded 
                    hover:bg-[#FFAD0D] transition-colors duration-200">
                    Submit Application
            </button>
        </div>
    </div>
</div>